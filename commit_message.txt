feat(community): fix comments crash, nested replies and full-screen story viewer

- Resolved TypeError by ensuring `handlePostComment` accepts the correct parameters directly from the component rather than relying exclusively on global scope/destructuring gaps in `page.tsx` vs `CommunityTab.tsx`.
- Refactored `handlePostComment` to properly handle and forward `parent_id` making the backend Supabase integration work out of the box.
- Indented child replies within the UI with `ml-8` and `border-l-2`.
- Corrected CSS overflow issue for the Story Preview Modal by applying `max-h-[90vh] overflow-y-auto`, ensuring mobile usability and the visibility of the Publish button.
- Built a completely new Full-Screen immersive Story Viewer within `CommunityTab.tsx` directly linked to the user's interaction state (`viewerActiveGroupIndex`). Includes logic for progress tracking, pausing/resuming stories, and interacting via Reactions which are now fully linked to Supabase database.
